-# edit.html.haml using partial
 
%h2 Edit User
 
= form_tag user_path(@user), :method => :put do
  = render :partial => 'user_form'
  = submit_tag 'Update Movie Info'

  -#= button_tag "Cancel", :type => 'cancel', :name => 'cancel' # 4.4 the cancel linkthat goes back to the all movies page
  = button_tag "Cancel", :type => 'cancel', :onclick => user_path(@user) , :name => 'cancel' # 4.4 the cancel linkthat goes back to the all movies page
%br
  -#= link_to 'Cancel for 4.5', movie_path(@movie) # 4.5 the cancel link to go back to viewing page of the movie that calcel the edition
